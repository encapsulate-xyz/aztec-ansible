---
# maintains testnet inventory grouped by project names
all:
  vars:
    env: testnet
  children:
    aztec:
      hosts:
        sequencer.aztec.testnet.encapsulate.xyz:
          type: sequencer
